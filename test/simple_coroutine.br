fn my_main( p: bool) {
    c:i32 := init my_co(5, 13);
    w : i32 := yield c;
    x : i32 := 1;
    printiln x;
    printiln w;
    z : i32 := yield c;
    printiln z;
    return;
}

co my_co (x: i32, y: i32) -> i32 {
    yret x;
    return y;
}