
fn my_main() -> i64 {
    let a: i64 := 1;
    let b: i64 := 2;
    test(a, a);

    return 0;
}

fn test(a: i64, b: i64) {
    root::std::io::writei64ln(a);
    root::std::io::writei64ln(b);

    return;
}