extern fn printf(fmt: string, ...);

mod math {
    /*
    My funky test module
    */
    mod arithmetic {
        // Does something
        fn something(m: super::S) -> i64 {
            while (!(-m.x < 4)) {
                foo();
            };
            
            return 5;
        }

        fn foo() {
            return;
        }
    }

    struct S {
        x: i64,
    }
}