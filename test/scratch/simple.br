extern fn printf(fmt: string, ...);

mod math {
    /*
    My funky test module
    */
    mod arithmetic {
        // Does something
        fn something() -> i64 {
            let a: [i64; 2] := foo();
            return 5;
        }

        fn foo() -> [i64; 2] {
            return [0, 3];
        }
    }

    struct S {
        x: i64,
    }

    struct S2 {
        s: S,
    }
}