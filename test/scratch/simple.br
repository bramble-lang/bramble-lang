extern fn printf(fmt: string, ...);

mod math {
    /*
    My funky test module
    */
    mod arithmetic {
        // Does something
        fn something(m: super::S) -> i64 {
            let s: string := "hello";
            project::std::io::write(s);
            while (!(-m.x < 4 + 2)) {
                foo();
            };
            
            return 5;
        }

        fn foo() {
            let s2: super::S2 := super::S2{ s: super::S{ x: 5}};
            return;
        }
    }

    struct S {
        x: i64,
    }

    struct S2 {
        s: S,
    }
}