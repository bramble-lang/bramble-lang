extern fn puts(s: string);

fn my_main() -> i64 {
    let mut x: i64 := 0;
    while (x < 5) {
        project::std::io::writei64(x);
        project::std::io::write(" hello, world\n");
        mut x := x + 1;
    };
    return 0;
}