extern fn printf(fmt: string, ...);

mod math {
    /*
    My funky test module
    */
    mod arithmetic {
        // Does something
        fn something(m: super::S) -> i64 {
            let s2: super::S := get_s();
            
            return s2.x;
        }

        fn get_s() -> super::S {
            return super::S {
                x: get_i(),
            };
        }

        fn get_i() -> i64 {
            return 5;
        }
    }

    struct S {
        x: i64,
    }
}