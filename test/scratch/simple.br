extern fn puts(s: string);

fn my_main() -> i64 {
	let a:[[i64;2]; 1] := [[1, 2]];
	let x: i64 := a[0][1];
	root::std::io::writei64ln(x);

	let a2: [[i64; 2]; 2] := [[1, 2,], [3, 4],];
	root::std::io::writei64ln(a2[0][0]);
	root::std::io::writei64ln(a2[0][1]);
    return 0;
}

fn blah(a: [i64; 2]) -> [i64; 2] {
	return a;
}