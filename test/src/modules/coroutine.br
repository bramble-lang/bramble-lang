fn my_main() {
    let c: co i64 := init my_mod::count(3);
    printiln (yield c);
    printiln (yield c);
    printiln (yield c);

    return;
}

mod my_mod {
    fn my_bool() -> bool {
        let b:bool := false;
        return b;
    }

    fn add(a: i32, b: i32) -> i64 {
        return a + b;
    }

    co count(start: i32) -> i64 {
        yret start + 1;
        yret start + 2;
        return start + 3;
    }
}