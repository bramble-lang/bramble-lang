fn my_main() {
    let x:i32 := my_mod::add(3, 2);
    printiln x;

    let b:bool := my_mod::my_bool();
    printbln b;

    let c: co i32 := init my_mod::count();
    printiln (yield c);
    printiln (yield c);
    printiln (yield c);

    return;
}

mod my_mod {
    fn my_bool() -> bool {
        let b:bool := false;
        return b;
    }

    fn add(a: i32, b: i32) -> i32 {
        return a + b;
    }

    co count() -> i32 {
        yret 1;
        yret 2;
        return 3;
    }
}