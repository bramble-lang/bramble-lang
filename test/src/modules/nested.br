fn my_main() {
    let x:i64 := my_mod::inner::add(3, 2);
    printiln x;

    let b:bool := root::my_mod::is_3(1);
    printbln b;

    let b2:bool := root::my_mod::is_3(3);
    printbln b2;
    
    let y:i64 := self::my_mod::inner::add(7, 2);
    printiln y;

    return;
}

mod my_mod {
    fn is_3(i: i32) -> bool {
        let x: i64 := self::inner::add(1, 2);
        return i == x;
    }
    
    mod inner {

        fn add(a: i32, b: i32) -> i32 {
            return a + b;
        }
    }
}