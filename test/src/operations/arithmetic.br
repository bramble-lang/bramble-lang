
fn my_main() {
    let add:i64 := 5 + -2;
    let sub:i64 := add - 2;
    let negative:i64 := 0 - sub;
    let multiply:i64 := 5 * -negative;
    let divide:i64 := multiply / 5;
    root::std::io::writei64ln(add);
    root::std::io::writei64ln(sub);
    root::std::io::writei64ln(negative);
    root::std::io::writei64ln(multiply);
    root::std::io::writei64ln(divide);
    return;
}