
fn my_main() -> i64 {
    let add:i64 := 5 + -2;
    let sub:i64 := add - 2;
    let negative:i64 := 0 - sub;
    let multiply:i64 := 5 * -negative;
    let divide:i64 := multiply / 5;
    project::std::io::writei64ln(add);
    project::std::io::writei64ln(sub);
    project::std::io::writei64ln(negative);
    project::std::io::writei64ln(multiply);
    project::std::io::writei64ln(divide);
    return 0;
}