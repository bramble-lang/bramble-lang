fn my_main() -> i64 {
    let a:bool := 5 < 6;
    let b:bool := 6 < 5 || 7 <= 5 + 1*2;
    let c:bool := 2 != 3 && 2 != 4;
    let d:bool := 2 < 3 || 3 < 2 && 2 > 4 && 2 <= 1;
    let e: bool := 2 > 3 || 2 != 2;
    let f:bool := a && b && c && d || e;
    root::std::io::writeboolln(a);
    root::std::io::writeboolln(b);
    root::std::io::writeboolln(c);
    root::std::io::writeboolln(d);
    root::std::io::writeboolln(e);
    root::std::io::writeboolln(f);
    return;
}