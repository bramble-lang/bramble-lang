fn my_main() {
    let a:bool := 5 < 6;
    let b:bool := 6 < 5 || 7 <= 5 + 1*2;
    let c:bool := 2 != 3 && 2 != 4;
    let d:bool := 2 < 3 || 3 < 2 && 2 > 4 && 2 <= 1;
    let e: bool := 2 > 3 || 2 != 2;
    let f:bool := a && b && c && d || e;
    printbln a;
    printbln b;
    printbln c;
    printbln d;
    printbln e;
    printbln f;
    return;
}