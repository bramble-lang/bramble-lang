fn my_main( p: bool) {
    let c: co i64 := init my_co(5, 13);
    let w : i64 := yield c;
    let x : i64 := 1;
    printiln x;
    printiln w;
    let z : i64 := yield c;
    printiln z;
    return;
}

co my_co (x: i64, y: i64) -> i64 {
    yret x;
    return y;
}