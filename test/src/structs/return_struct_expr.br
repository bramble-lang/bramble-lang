fn my_main() {
    let y: MyStruct := test4(MyStruct{ x: 10, y: 100, z: 20}, MyStruct{ x: 10, y: 100, z: 20});
    printiln y.x;
    printiln y.y;
    printiln y.z;
    
    return;
}

struct MyStruct {
    x: i32,
    y: i32,
    z: i32,
}

fn test4(x: MyStruct, y: MyStruct) -> MyStruct {
    return MyStruct{
        x: x.x + y.x,
        y: x.y + y.y,
        z: x.z + y.z,
    };
}