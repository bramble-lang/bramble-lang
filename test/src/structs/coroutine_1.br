
fn my_main( p: bool) {
    let ms:MyStruct := MyStruct {x: 5, y: 13, z: true};
    let c:i32 := init my_co(ms, ms);
    let w : i32 := yield c;
    let x : i32 := 1;
    printiln x;
    printiln w;
    let z : i32 := yield c;
    printiln z;
    return;
}

co my_co (x: MyStruct, y: MyStruct) -> i32 {
    yret x.y + y.y;
    return x.x;
}

struct MyStruct {
    x: i32,
    y: i32,
    z: bool,
}