fn my_main() -> i64 {
    let t: MyStruct := MyStruct{
        x: 5, 
        y: 1, 
    };
    root::std::io::writei64ln(t.x);
    root::std::io::writei64ln(t.y);
    root::std::io::writeboolln(t.z);
    
    return 0;
}

struct MyStruct {
    x: i64,
    y: i64,
    z: bool,
}