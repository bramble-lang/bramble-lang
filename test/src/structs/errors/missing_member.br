fn my_main() {
    let t: MyStruct := MyStruct{
        x: 5, 
        y: 1, 
    };
    root::std::io::writei64ln(t.x);
    root::std::io::writei64ln(t.y);
    printbln t.z;
    
    return;
}

struct MyStruct {
    x: i64,
    y: i64,
    z: bool,
}