fn my_main( p: bool) {
    let ms:MyStruct := MyStruct {x: 5, y: 13, z: true};
    let ms2:MyStruct := MyStruct {x: 123, y: 456, z: false};
    let c: MyStruct := init my_co(ms, ms);
    let w : MyStruct := yield c;
    let x : i32 := 1;
    printiln x;
    printiln w.x;
    let z : MyStruct := yield c;
    printiln z.y;
    return;
}

co my_co (x: MyStruct, y: MyStruct) -> MyStruct {
    printiln x.x;
    yret x;
    printiln y.x;
    return y;
}

struct MyStruct {
    x: i32,
    y: i32,
    z: bool,
}