fn my_main() {
    let ms: MyStruct := MyStruct {
        x: 1,
        y: 2,
        z: 3,
    };

    root::std::io::writei64ln(ms.x);
    root::std::io::writei64ln(ms.y);
    root::std::io::writei64ln(ms.z);
    let ms2: MyStruct := ms;
    root::std::io::writei64ln(ms2.x);
    root::std::io::writei64ln(ms2.y);
    root::std::io::writei64ln(ms2.z);


    return;
}

struct MyStruct {
    x: i64,
    y: i64,
    z: i64,
}