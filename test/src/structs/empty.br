fn my_main() {
    let t: MyStruct := MyStruct{};
    let y: MyStruct := test(t);
    root::std::io::writei64ln(2);
    return;
}

struct MyStruct {
}

fn test(e: MyStruct) -> MyStruct {
    let t: MyStruct := e;
    root::std::io::writei64ln(1);
    return t;
}