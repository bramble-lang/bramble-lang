fn my_main() -> i64 {
    let t: MyStruct := MyStruct{};
    let y: MyStruct := test(t);
    project::std::io::writei64ln(2);
    return 0;
}

struct MyStruct {
}

fn test(e: MyStruct) -> MyStruct {
    let t: MyStruct := e;
    project::std::io::writei64ln(1);
    return t;
}