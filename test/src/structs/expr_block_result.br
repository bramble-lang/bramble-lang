fn my_main() {
    let t: MyStruct := if (true) {
        let x:i32 := 5;
        let y:i32 := 1;
        MyStruct{x: x, y: y, z: true}
    } else {
        MyStruct{x: 0, y: -1, z: false}
    };
    printiln t.x;
    printiln t.y;
    printbln t.z;
    
    return;
}

struct MyStruct {
    x: i32,
    y: i32,
    z: bool,
}