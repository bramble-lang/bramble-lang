fn my_main() {
    let s: string := "cadabra\n";
    let c:co string  := init my_co("abra\n", s);
    prints (yield c);
    prints (yield c);
    prints (yield c);
    return;
}

co my_co(a: string, b: string) -> string {
    yret a;
    yret b;
    return "presto\n";
}