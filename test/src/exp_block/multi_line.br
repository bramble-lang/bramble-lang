fn my_main() -> i64 {
    let x:i64 := {
        let x: i64 := 1;
        let y: i64 := 4;
        x + y
    };
    root::std::io::writei64ln(x);

    let b:bool := {
        let b: bool := true;
        b && my_bool()
    };
    root::std::io::writeboolln(b);
    return 0;
}

fn my_bool() -> bool {
    let b:bool := false;
    return b;
}