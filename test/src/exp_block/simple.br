fn my_main() -> i64 {
    let x:i64 := {5};
    project::std::io::writei64ln(x);

    let b:bool := {my_bool()};
    project::std::io::writeboolln(b);
    return 0;
}

fn my_bool() -> bool {
    let b:bool := false;
    return b;
}