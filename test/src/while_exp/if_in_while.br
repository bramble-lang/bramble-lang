fn my_main() -> i64 {
    let mut x: i64 := 0;
    while (x <= 5) {
        if ((x > 1) && (x < 4)) {
            project::std::io::writei64ln(x);
        };
        mut x := x + 1;
    };
    return 0;
}