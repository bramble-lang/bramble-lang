fn my_main() -> i64 {
    test(0);
    test(1);
    return 0;
}

fn test(n: i64) {
    let mut x: i64 := 0;
    while (if (n == 0) {x <= 5} else {x <= 10}) {
        root::std::io::writei64ln(x);
        mut x := x + 1;
    };

    return;
}