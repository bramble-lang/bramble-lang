fn my_main() -> i64 {
    let mut x: i64 := 0;
    while (x <= 5) {
        let mut y: i64 := 0;
        while (y <= 2) {
            project::std::io::write("y: ");
            project::std::io::writei64ln(y);
            mut y := y + 1;
        };
        project::std::io::write("x: ");
        project::std::io::writei64ln(x);
        mut x := x + 1;
    };
    return 0;
}