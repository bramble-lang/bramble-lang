extern fn puts(s: string);

fn my_main() -> i64 {
    test(2);
    test(5);
    return 0;
}

fn test(n: i64) {
    let mut x: i64 := 0;
    while (x <= n) {
        root::std::io::writei64ln(x);
        mut x := x + 1;
    };

    return;
}