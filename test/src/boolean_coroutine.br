fn my_main() {
    c:bool := init my_co(true, 5, 13);

    printbln (yield c);
    printbln (yield c);
    printbln (yield c);

    return;
}

co my_co (b: bool, x: i32, y: i32) -> bool {
    yret b;
    yret x < y;
    return false;
}