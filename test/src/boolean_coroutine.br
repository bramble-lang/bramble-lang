fn my_main() {
    let c:co bool := init my_co(true, 5, 13);

    printbln (yield c);
    printbln (yield c);
    printbln ((yield c) == (yield c));

    return;
}

co my_co (b: bool, x: i64, y: i64) -> bool {
    yret b;
    yret x < y;
    yret true;
    return false;
}