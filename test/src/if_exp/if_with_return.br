fn my_main() -> i64 {
    root::std::io::writei64ln(test(0));
    root::std::io::writei64ln(test(20));
    return 0;
}

fn test(x: i64) -> i64 {
    let x:i64 := if (x < 10) {
        0
    } else {
        1
    };

    return x;
}