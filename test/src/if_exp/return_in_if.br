fn my_main() {
    root::std::io::writei64ln(test(0));
    root::std::io::writei64ln(test(20));
    return;
}

fn test(x: i64) -> i64 {
    if (x < 10) {
        return 0;
    } else {
        return 1;
    };

    return -1;
}