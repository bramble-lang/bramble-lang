fn my_main() -> i64 {
    test(2);
    test(11);
    test(5);
    test(127);
    test(128);
    test(255);
    test(256);
    test(500);
    test(5000);
    test(65537);
    test(65551);

    return 0;
}

fn test(a: i64) {
    if (a < 10) {
        project::std::io::writei64ln(a)
    };

    return;
}