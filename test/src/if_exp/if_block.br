fn my_main() {
    let x:i64 := test(2);
    let y:i64 := test(11);
    printiln x;
    printiln y;
    return;
}

fn test(a: i64) -> i64 {
    let x:i64 := if (a < 10) {
        printiln 0;
        let i:i64 := a * 2;
        i
    } else {
        printiln 1;
        let j:i64 := a * 2;
        j + a
    };

    return x;
}