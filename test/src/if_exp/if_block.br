fn my_main() {
    let x:i32 := test(2);
    let y:i32 := test(11);
    printiln x;
    printiln y;
    return;
}

fn test(a: i32) -> i32 {
    let x:i32 := if (a < 10) {
        printiln 0;
        let i:i32 := a * 2;
        i
    } else {
        printiln 1;
        let j:i32 := a * 2;
        j + a
    };

    return x;
}