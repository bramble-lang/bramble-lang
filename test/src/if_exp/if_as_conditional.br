fn my_main() {
    root::std::io::writei64ln(if_in_conditional(true));
    root::std::io::writei64ln(if_in_conditional(false));
    return;
}

fn if_in_conditional(b:bool) -> i64 {
    let x:i64 := if (if (b) {false} else {true}) {0} else {7};
    return x;
}