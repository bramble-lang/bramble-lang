fn my_main( p ) {
    c := init my_co(5, 13);
    w : i32 := yield c;
    x : i32 := 1;
    println x;
    println w;
    z : i32 := yield c;
    println z;
    return x + p;
}

co my_co (x: i32, y: i32) -> i32 {
    yret x;
    return y;
}