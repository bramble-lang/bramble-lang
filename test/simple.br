fn my_main( p ) -> i32 {
    c := init my_co(5, 13);
    w : i32 := yield c;
    x : i32 := 1;
    printiln x;
    printiln w;
    z : i32 := yield c;
    printiln z;
    return x + p;
}

co my_co (x: i32, y: i32) -> i32 {
    yret x;
    return y;
}