fn my_main( p ) {
    c := init my_co(5, 13);
    w := yield c;
    x := 1;
    println x;
    println w;
    z := yield c;
    println z;
    return x + p;
}

co my_co (x, y) {
    yret x;
    return y;
}